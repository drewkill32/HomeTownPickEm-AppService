import Schedule from './Schedule';
import PicksHome from './picks/PicksHome';

const WeeklyStats = () => {
  return (
    <div>
      <Schedule />
      <PicksHome />
    </div>
  );
};

export default WeeklyStats;
